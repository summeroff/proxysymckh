{
  "name": "proxysymckh",
  "version": "1.1.0",
  "description": "Local symbol-server proxy: map S3 403→404 and never emit chunked responses (symchk/VS).",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
